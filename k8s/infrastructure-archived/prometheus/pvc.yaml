# ============================================================
# Prometheus PersistentVolumeClaim
# ============================================================
# Uses GKE standard-rwo StorageClass for persistent metrics storage
# ============================================================

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prometheus-pvc
  namespace: lgtm
  labels:
    app: prometheus
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
